# TPC 控制

版本：`v16.1.260606.2115`

TPC Control 是激光二极管电流控制工作区。

## 功能

- 红色和绿色激光通道控制。
- Keithley 电流输出。
- 电流限制。
- 基本状态/log 显示。

## 安全要求

任何可以发送到硬件的电流都必须经过限制检查。不要为了 UI 便利绕过电流检查，也不要在 restore 中恢复硬件输出 ON 状态。

## 维护建议

如果未来把 TPC 运行逻辑抽到 `runtime/` 或 `hardware/`，先补非硬件测试，并确保串口命令顺序和 STOP 行为不改变。
